import Foundation

@MainActor
final class GlobalHotkeyManager {
    func start() {
        // Phase 1 initial skeleton: real registration will be wired
        // after the panel lifecycle is stable.
    }
}
